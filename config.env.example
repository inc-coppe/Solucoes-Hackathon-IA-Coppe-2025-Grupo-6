# Configurações de Segurança - API GIV
# Copie este arquivo para config.env e configure suas chaves secretas

# Chave secreta para JWT da API GIV (GERE UMA CHAVE FORTE!)
GIV_SECRET_KEY=sua-chave-secreta-forte-aqui

# Tempo de expiração do token JWT (em minutos)
GIV_ACCESS_TOKEN_EXPIRE=30

# Chave secreta para a API anterior (app.py)
APP_SECRET_KEY=sua-chave-secreta-app-aqui

# Configurações do Banco de Dados (se necessário)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Configurações de Log
LOG_LEVEL=INFO

# Configurações de Ambiente
ENVIRONMENT=development
